I've added a login page,
to run the project =>>
download and extract the zip, 
open the folder with visual studio code, 
click on the top left three dots > terminal > type: "python manage.py runserver", click on the link in therminal with ctrl + left click
access the html files in flashcards > templates > login.html
access the .css files in flashcards > static > flashacards > login_css.css (styles.css)

when making changes to the html and css, ctrl + s to save changes, and the server automatically refreshes

====================================
NEED THE FOLLOWING CHANGES: 
====================================
connecting the login.html page with mysql database, 

MySQL required, with the script in the repo flashed!